//! `RemoteX` agent composition root. Runtime behavior begins in later milestones.

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    println!("RemoteX agent — Milestone 0 foundation");
    Ok(())
}
