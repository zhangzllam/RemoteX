//! `RemoteX` control-server composition root. APIs and persistence are outside M0.

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    println!("RemoteX control server — Milestone 0 foundation");
    Ok(())
}
