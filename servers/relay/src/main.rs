//! `RemoteX` relay composition root. Pairing and forwarding are outside M0.

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    println!("RemoteX relay server — Milestone 0 foundation");
    Ok(())
}
